# claude-skills

A collection of custom skills for Claude Code.

## Structure

Each skill lives in its own directory with a `SKILL.md` describing what it does and when to use it.

## Usage

Clone this repo and point Claude Code at the skills you want, or symlink them into `~/.claude/skills/`.
